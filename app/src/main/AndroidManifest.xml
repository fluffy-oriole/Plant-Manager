<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/plant_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/plant_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlantManager" >
        <activity
            android:name=".controller.AddNewType"
            android:exported="false"
            android:theme="@style/Theme.PlantManager" />
        <activity
            android:name=".controller.PlantsListActivity"
            android:exported="false" />
        <activity
            android:name=".controller.AssessmentActivity"
            android:exported="false" />
        <activity
            android:name=".controller.ScheduleActivity"
            android:exported="false" />
        <activity
            android:name=".controller.EditActivity"
            android:exported="false" />
        <activity
            android:name=".controller.AddPlant"
            android:exported="false" />
        <activity
            android:name=".controller.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>